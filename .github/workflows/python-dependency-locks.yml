name: Python Dependency Locks

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  dependency-locks:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        # v5
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd

      - name: Install lock tooling
        uses: validityBase/vbase-github-actions/.github/actions/setup-python-deps@v1
        with:
          requirements-files: |
            requirements-lock.txt
          python-version: "3.11"
          require-hashes: "true"

      - name: Verify generated lock files are current
        run: |
          pip-compile --strip-extras --no-annotate --generate-hashes -o requirements.txt requirements.in
          pip-compile --strip-extras --no-annotate --allow-unsafe --generate-hashes -o requirements-dev.txt requirements-dev.in
          pip-compile --strip-extras --no-annotate --generate-hashes -o docs/requirements.txt docs/requirements.in
          pip-compile --strip-extras --no-annotate --allow-unsafe --generate-hashes -o requirements-lock.txt requirements-lock.in
          git diff --exit-code -- requirements.txt requirements-dev.txt requirements-lock.txt docs/requirements.txt
