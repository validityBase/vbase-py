-r ../requirements.in

aiohttp>=3.14.3

# SQLAlchemy requires greenlet on Linux. Keep it explicit in terminal locks so
# hash-checked installs work across macOS lock regeneration and Ubuntu CI.
greenlet>=3.5.4

# PostgreSQL driver required by SQLIndexingService. Not in requirements.in because
# vbase is an intermediate library; downstream apps choose their own driver.
psycopg2-binary>=2.9.0
black>=26.5.1
pre-commit>=4.6.1
PyYAML>=6.0.2
pylint>=4.0.6
-r tools.in
